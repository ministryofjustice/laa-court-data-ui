---
en:
  activemodel:
    errors:
      models:
        search:
          attributes:
            dob:
              blank: Defendant date of birth required
              invalid: Enter a valid defendant date of birth
            filter:
              blank: Filter required
            term:
              blank: Search term required
              invalid: Search term must contain only letters, numbers and dash ('-')
              too_short: Search term must be at least %{count} characters
