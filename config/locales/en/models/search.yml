---
en:
  activemodel:
    errors:
      models:
        search:
          attributes:
            dob:
              blank: Defendant date of birth required
            filter:
              blank: Filter required
            term:
              blank: Search term required
              invalid: Search term must contain only letters and numbers
              too_short: Search term must be at least %{count} characters
