---
en:
  activemodel:
    errors:
      models:
        unlink_attempt:
          attributes:
            reason_code:
              blank: Choose a reason for unlinking
              inclusion: Choose a reason for unlinking from list
            other_reason_text:
              blank: Enter the reason for unlinking
